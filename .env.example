# Jarvis Core v2 local configuration template.
# Copy to .env only when a later milestone introduces configuration that needs it.
# Never commit real secrets.

JARVIS_ENV=development
# OPENAI_API_KEY=replace_me_in_local_env_only
