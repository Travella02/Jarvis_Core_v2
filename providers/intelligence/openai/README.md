# OpenAI intelligence provider

Reserved for milestone **0.0.2 - Intelligence Provider**.

GPT-5.6 Luna will initially be configured here behind `core.intelligence.IntelligenceProvider`. No OpenAI SDK dependency or model-specific schema belongs in `core/`.
